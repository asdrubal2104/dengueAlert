# Project Rules & Guidelines

## Package Management
- **Package Manager**: Always use `pnpm` exclusively for all package management, script execution, builds, and development commands in this repository (e.g. `pnpm run build`, `pnpm run dev`, `pnpm test`, `pnpm add`).
- Do NOT use `npm` or `yarn`.
